{
	"name": "omp-jevens-classifier",
	"version": "0.3.0",
	"description": "Jev-powered permission gate for OMP: intercepts bash calls and spawn-bearing eval payloads the host would auto-approve, asks TypeSafe System One (jev-latest) typed questions about them — one verdict choice, one noul per hazard, one blast-radius score — and decides from the returned probabilities instead of parsing model text.",
	"type": "module",
	"private": true,
	"scripts": {
		"test": "bun test",
		"typecheck": "bunx tsc -p tsconfig.json"
	},
	"engines": {
		"bun": ">=1.3.14"
	},
	"omp": {
		"extensions": [
			"./index.ts"
		]
	},
	"devDependencies": {
		"@oh-my-pi/pi-ai": "18.2.4",
		"@oh-my-pi/pi-coding-agent": "18.2.4",
		"@oh-my-pi/pi-utils": "18.2.4",
		"@types/bun": "^1.3.14",
		"typescript": "^7.0.2"
	}
}
